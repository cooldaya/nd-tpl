const MONIT_INJECT_KEY = '__monitor_inject__';

const VIDEO_NODE_TYPE = 'video';


const EVENT_TYPES = {
  UPDATE_TREE_SELECTED:'UPDATE_TREE_SELECTED'
}
const PLAY_FUNC_MODES = {
  PREVIEW:'preview',
  PLAYBACK:'playback'
}


export {
  MONIT_INJECT_KEY,
  EVENT_TYPES,
  VIDEO_NODE_TYPE,
  PLAY_FUNC_MODES
}